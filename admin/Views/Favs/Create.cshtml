@model admin.Models.Fav

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Fav</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="商品id" class="control-label"></label>
                <select asp-for="商品id" class ="form-control" asp-items="ViewBag.商品id"></select>
            </div>
            <div class="form-group">
                <label asp-for="會員id" class="control-label"></label>
                <select asp-for="會員id" class ="form-control" asp-items="ViewBag.會員id"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
